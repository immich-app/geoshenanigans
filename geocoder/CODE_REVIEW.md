# Geocoder code-quality review (2026-06-17)

Parallel multi-agent review of the builder (C++) + server (Rust), for the refactor pass on `refactor/geocoder-quality`.
Hard invariants every refactor must preserve: **(1) determinism** — chained build from identical PBF reproduces a byte-identical index (same-PBF planet patch ~243 B, verify 26/26); **(2) Nominatim parity** — 500-city query results unchanged. HIGH/MEDIUM findings only (low-severity + full text were in the agent output).

## Refactors applied in this pass (validated)

Every change below was validated against the hard invariants before commit. "byte-identical" means a same-binary build was diffed file-by-file against a pre-change baseline (`cmp -s` over every output `.bin`); "planet" includes TIGER + wikidata sitelinks. The two builder refactors are determinism-critical, so both were proven byte-identical at oceania **and** planet scale — byte-identical output is the strongest possible proof a refactor changed no behaviour (it also implies Nominatim parity is untouched).

- **Test infrastructure (was: none).** Added a dependency-free C++ unit harness (`tests/test_framework.h` + `tests/*.cpp`, a `geocoder-tests` CMake target) — 99 tests / 391 checks over the builder's pure logic (id_allocator, postcode, geometry, string_pool, ring_assembly, interpolation, patch_format). Added Rust `#[cfg(test)]` coverage to the server (6 → 49 tests) over the query-path pure helpers. No production code touched by the test commits.
- **`cell_item_less` hoist** (`16eec17`). The cell→item index sort tiebreak (cell_id, then item_id) was an identical inline lambda copy-pasted in 7 ordering/cell-build sites; hoisted to one `cell_item_less()` in `types.h`. _Validated byte-identical: oceania 131/131, planet 131/131._
- **`classify_poi` extraction** (`48a47d3`). The ~350-line captureless POI-classifier lambda was lifted out of `main()` to a file-scope `static` free function (reusing the existing `PoiClassification` struct from `types.h`, which the inline copy was shadowing). Addresses the "classify_poi lambda" HIGH item below. _Validated byte-identical: oceania 131/131, planet 131/131._
- **`float_bits` hoist + re-inlined `cell_item_less` cleanup** (`05b22b9`). Lifted the IEEE-754 total-order `float_bits` lat/lng tiebreak (used by all 6 deterministic-ordering sort comparators) from a `main()` lambda to a file-scope inline function, and replaced the two POI/addr parent-street `lower_bound` probes that re-inlined the `cell_item_less` comparator with the hoisted helper. _Validated byte-identical: oceania 131/131, planet 131/131._
- **Byte-safe code-quality polish** (`0b654c3`). Replaced the scattered raw `0x7FFFFFFF`/`0x80000000` admin cell-id literals with the existing `ID_MASK`/`INTERIOR_FLAG` constants (dropping local `ADMIN_*` duplicates; left the `float_bits` IEEE-754 sign-bit literal — same value, different meaning); named the FNV-1a basis/prime (`FNV1A_OFFSET_BASIS`/`FNV1A_PRIME`); and made the TIGER/postcode loaders check the `system()` (tar/gunzip) return code and bail instead of silently proceeding. Addresses the three MEDIUM items so marked below. _Validated byte-identical: oceania 131/131, planet 131/131._
- **`decode_polygon_verts` F32 alignment UB fix** (`8190e6f`, server). The encoding-4 branch cast a 2-aligned byte pointer to `*const NodeCoord` (needs 4-byte alignment) — undefined behaviour. Replaced with per-field unaligned `f32::from_le_bytes` reads (bit-identical on little-endian) + added a test. Risk-free: the builder only ever emits encodings 0/1/2/3, so encoding 4 is a dead/legacy reader branch — cannot affect any current query result. Addresses the first "Suspected pre-existing bugs" item below.

### Remaining structural refactors — deliberately left for human-supervised application

These are high-value but either invasive or judgment-dependent, so they are documented (below) rather than auto-applied. All are byte-validatable with the same oceania→planet `cmp` methodology, which makes them *safe to attempt* — the reason to hold them is that they need a human to drive the design / confirm intent, not that they're unverifiable:

- **`main()` decomposition** (HIGH, below). ~5150 lines. The blocker isn't risk (byte-validatable) — it's the design decision of how to thread the large shared state (`ParsedData`, allocators, dozens of parallel vectors) through extracted phase functions (param lists vs a context struct). A reviewer should choose the seam.
- **Parent-linking / containment dedup** (HIGH, below). The 4 sweeps (admin-containment, way-parent, POI-parent, centroid-postcode) share a skeleton but have likely-intentional per-site differences (tie-breaks, candidate sources). A naive unification would either fail byte-identical or need so many flags it isn't cleaner — a human should confirm which differences are essential before unifying. (Note: the PIP *primitive* is already factored into `point_in_polygon`/`poly_contains`; this is about the surrounding loops.)
- Smaller captureless lambdas (`place_rank`, `float_bits`, `addr_equal`, `quality_dir_name`, `area`, `parallel_sort_and_build`, `rebuild_map_parallel`) — extracting them is a readability wash relative to the per-change validation cost; fold them in opportunistically alongside the main() decomposition.

## builder/build_index.cpp (main pipeline, 5966 lines)
Orchestrates the entire planet-build pipeline: argument parsing, optional external-data loading (TIGER, GeoNames postcodes, wikidata sitelinks), a multi-threaded two-pass PBF parse (relations, streaming nodes, ways) into a dense node-location index, parallel ring assembly for admin boundaries and P…

**Refactor risk:** HIGH. This file is the most determinism- and results-sensitive in the builder. (1) The entire 'Deterministic ordering' pass (4734-5430) DEFINES the on-disk byte layout: the sort comparators, float_bits tiebreaks, osm_id final tiebreaks, dedup-survivor selection, and lockstep osm_id/cell remaps are exactly what makes the same-PBF patch ~243 bytes (26/26). The long inline comments document past det…

- **HIGH** [structure] `main() spanning lines 445-5965` — main() is ~5500 lines. The PBF passes, ring assembly, the four parent-linking PIP phases, the S2 covering, the 7-block deterministic ordering pass, and the write_region lambda are all inline. This is… → _Extract each '// --- ... ---' marked phase into a named free function taking ParsedData& plus the few needed params (e.g. parse_pbf(data, files, num_threads), assemble_admin_polygons(data, ...), link…_
- **HIGH** [duplication] `link_admin_parent_chain 3376-3448; way parent 3454-~3560; P…` — Four near-identical worker blocks implement the same algorithm: compute a point (centroid or POI lat/lng), get its L10 S2 cell + AppendAllNeighbors, scan cell_to_admin candidates, run an inline ray-c… → _Extract a helper find_smallest_containing_admin(data, lat, lng, predicate(cand)->bool) -> uint32_t that returns the best polygon id, and a free function for the PIP loop. Each phase becomes a small l…_
- **HIGH** [duplication] `Inline PIP ray-cast at 3424-3428, 3531-3535, 4209-4213, 429…` — The point-in-polygon ray-casting loop is hand-inlined 4 times with identical float arithmetic, even though point_in_polygon / point_in_polygon_nc helpers already exist and are used elsewhere (3194, 3… → _Add/reuse a point_in_polygon_nc(lat,lng, const NodeCoord* verts, uint32_t cnt) overload and call it in all four sites. RISK: results-sensitive (geocoding parity). The inline form uses verts[].lng/lat…_
  - ⚠️ **CORRECTION (verified against current code):** this recommendation is WRONG and **not byte-safe**. The existing `point_in_polygon_nc` (continent_boundaries.h:40) crosses on the **lat** axis (`(yi>lat)!=(yj>lat)`), but all the inline admin-parent sites cross on the **lng** axis (`(verts[a].lng>clng)!=(verts[b].lng>clng)`). Mathematically valid but different float bits at edges → would change which polygon wins → breaks determinism AND parity. The place-node containment at ~3198 deliberately uses the lat-axis helper; the 4 admin-parent sweeps deliberately use the lng-axis form. To dedup byte-identically you must add a NEW helper matching the lng-axis expression EXACTLY, then prove oceania+planet `cmp`. Current sites: 3429-3431, 4210-4212, 4298-4300 (way-parent already factored into a local `poly_contains` lambda ~3529).
- **HIGH** [duplication] (partially ✅ — `16eec17`) `Deterministic ordering blocks 2-7: addr 4771-4908, ways 491…` — Six sort+reorder+dedup+lockstep-osm_id+cell-remap blocks repeat the same skeleton (~80-100 lines each): build order[], std::sort by keys with float_bits tiebreak then osm_id, build old_to_new, reorde… → _Hoist float_bits and the CellItemPair cmp to file-scope helpers. Consider a templated reorder_and_remap_cells(order, old_to_new, sorted_cells) helper for the mechanical reorder+cell-remap tail. Keep …_ — **The CellItemPair cmp AND `float_bits` are now hoisted** (`16eec17`, `05b22b9`; both validated byte-identical), and the two re-inlined `cell_item_less` lower_bound probes now use the helper. Only the templated reorder+dedup+lockstep-remap tail remains (human-supervised — per-block parallel-array sets differ; needs a careful template + cmp proof).
- **HIGH** [structure] ✅ **DONE** (`48a47d3`) `classify_poi lambda 1066-~1419` — A ~350-line lambda with 20 const char* parameters performing a flat priority cascade of strcmp dispatch over OSM tags. As a captureless lambda inside main() it cannot be unit-tested and bloats the pa… → _Promote to a free function classify_poi(const PoiTags&) where PoiTags is a small struct of the tag pointers, in a poi_classify.{h,cpp} unit. Behavior is pure tag->category/tier/flags mapping. RISK: r…_ — **Applied** as a file-scope `static` free function (kept the 20-param signature rather than a PoiTags struct, to stay provably byte-identical; a follow-up could introduce the struct). Validated byte-identical oceania + planet.
- **HIGH** [structure] `write_region lambda 5476-5829 (also write_qualities 5445, n…` — write_region is a ~350-line lambda containing further nested lambdas and large inline blocks (admin-minimal filter/remap, POI tier filtering with inline vertex byte-packing/encoding selection). The i… → _Extract write_region, write_admin_minimal (already partially a helper write_admin_minimal_polygons), and a pack_poi_vertices(records, vertices, enc-selection) helper into the writer translation unit.…_
- **~~HIGH [bug]~~ → NOT A BUG (verified)** `load_tiger_data 341 (city_centroids is std::unordered_map);…` — TIGER place nodes are emitted by iterating `for (const auto& [city_name, acc] : city_centroids)` where city_centroids is a std::unordered_map. Iteration order of unordered_map is not guaranteed stabl… → _Collect into a std::map<std::string,CityAccum> or sort the keys before emission…_
  - ✅ **CORRECTION (traced end-to-end):** the unordered_map order does **not** reach the on-disk bytes. The synthetic id is an FNV hash of the city name (order-independent); `name_id` is the only order-dependent field, and `partition_strings_into_tiers` re-sorts the string pool by content and rewrites every place_node.name_id before any write; then the deterministic-ordering pass re-sorts place_nodes by (place_type, name_id, lat_bits, lng_bits, osm_id) with place_osm_ids reordered in lockstep. This is *why* planet output is byte-identical. A `std::map` swap is cheap defense-in-depth/self-documentation, not a correctness fix. Severity overstated; no fix required.
- **MEDIUM** [structure] `load_tiger_data 118-367` — Single function doing tar extraction, CSV discovery, a full parse pass building interp ways + postcodes, then a SECOND full re-read of every CSV (304-337) purely to accumulate city centroids. The two… → _Split into discover_tiger_csvs(), parse_tiger_ranges(), and accumulate_tiger_city_centroids(); share one csv_split helper. The second pass can be merged into the first to halve I/O. RISK: determinism…_
- **MEDIUM** [error-handling] ✅ **DONE** (`0b654c3`) `system(cmd.c_str()) at 127 and 381` — Both external-data loaders shell out (tar xzf / gunzip) via system() and ignore the return code. A failed extraction silently yields zero CSV files / an empty temp file, and the build proceeds produc… → _Check the system() return value and std::cerr + return early (or exit nonzero) on failure. Behavior-preserving for the success path. Note: invoking the shell with an unsanitized path is also a latent…_
- **MEDIUM** [magic-number] ✅ **DONE** (`0b654c3`) `Magic mask 0x7FFFFFFF / 0x80000000 / 0xFFFFFFFFu scattered …` — The admin cell-entry high-bit (INTERIOR_FLAG) and its complementary id mask are written as raw 0x7FFFFFFF / 0x80000000 literals in ~8 places, while named constants ADMIN_ID_MASK/ADMIN_INTERIOR_FLAG a… → _Define ADMIN_INTERIOR_FLAG / ADMIN_ID_MASK once at file or header scope and replace all raw literals. Pure rename, no byte impact._
- **MEDIUM** [duplication] ✅ **DONE** (`0b654c3`) `FNV-1a constants 14695981039346656037ULL / 1099511628211ULL…` — FNV-1a offset basis and prime are hand-inlined in two synthetic-id hashers (TIGER interp and TIGER place). These hashes are determinism-critical synthetic identities; duplicating the constants invite… → _Provide a small fnv1a(seed, bytes...) / fnv1a_str helper next to pack_osm_id and call it in both sites. RISK: determinism-sensitive but mechanical — the helper must reproduce the exact mix order; onc…_
- **MEDIUM** [structure] `POI importance computation 5331-5389 nested inside the POI …` — A ~60-line importance scoring computation (sitelink curve, peak/volcano elevation scaling, polygon-area shoelace scaling) is embedded inside the deterministic-sort block for POIs. It is pure per-reco… → _Extract compute_poi_importance(data, poi_elevations, poi_qids, lookup_sitelinks) as its own phase/function called after the POI sort. RISK: results-sensitive (importance affects ranking) and the floa…_

_Tests:_ classify_place_override (64-89) and place_type_to_admin_override (95-114) (easy); pack_osm_id (41) + a proposed fnv1a helper (easy); float_bits total-order key (4761-4766) (easy); classify_poi (after extraction to a free function) (moderate); Extracted point_in_polygon_nc over NodeCoord and find_smallest_containing_admin predicate selection (moderate); TIGER WKT LINESTRING parser and CSV field splitter in load_tiger_data (172-219) (moderate); quality_dir_name (5436-5440) (easy)

## builder/cell_index.cpp
Writes the compact binary index from ParsedData: cell→entry tables (geo/admin/postcode), record arrays (ways, addr_points, admin/postal polygons, POIs, places, interp), per-tier string pools, and quality/admin-minimal polygon variants. Also runs the strategy-2 stable-id pass (apply_strategy2_*): fo…

**Refactor risk:** HIGH. This module directly determines on-disk byte layout (record arrays, cell/entry tables, packed vertex streams, string tiers) and the stable-id ordering that the daily patch toolchain depends on — it is the heart of the byte-identical determinism guarantee (same-PBF planet patch ~243 bytes, 26/26). Any change to: allocation order in the strategy-2 allocate loops; the order/conditions of refer…

- **HIGH** [duplication] `apply_strategy2_streets/admins/addrs/places/pois/interps (l…` — Six near-identical functions share the same skeleton: empty/size-mismatch guard, IdAllocator load_previous, allocate-loop computing remap[] and identity flag, n_new==n_old fast-path with logging + ta… → _Extract a template/helper that owns the invariant skeleton: a function taking the osm_ids vector, record vector, a list of parallel arrays to reorder, a list of reference-site visitors (plain vs flag…_
- **HIGH** [structure] `postcode centroid write block (lines 1091-1343)` — A single std::async lambda spanning ~250 lines does five distinct jobs: per-country re-accumulation by scanning addr_points (1121-1146), merging external postcode_accum entries (1152-1175), validatio… → _Decompose into named helpers: accumulate_postcode_centroids(data)->vector<PostcodeCentroid>, then reuse the unified strategy-2 helper (see first finding) for the postcode case, then write_postcode_in…_
- **MEDIUM** [duplication] `map_ref / map_ref_flagged lambdas (lines 297-299, 405-419, …` — The plain map_ref lambda (guard NO_DATA and v<remap.size(), assign remap[v]) is redefined verbatim in 6 functions; the INTERIOR_FLAG-aware map_ref_flagged is defined twice (admins line 414, pois line… → _Hoist two free helpers (e.g. remap_index(uint32_t&, const std::vector<uint32_t>&) and remap_index_flagged(...)) used by all callers. Pure index arithmetic, byte-output identical, low determinism risk._
- **MEDIUM** [duplication] `street_nodes rebuild (lines 277-294) and interp_nodes rebui…` — Two byte-identical loops rebuild a NodeCoord array in record order with sequential node_offsets, including the same out-of-range guard that zeroes node_offset/node_count. Only the field types (WayHea… → _Extract a templated helper rebuild_child_nodes(records, src_nodes) -> new_nodes that mutates node_offset/node_count in place. Behavior-preserving; determinism-sensitive (node_offset assignment drives…_
- **MEDIUM** [duplication] `pack_polygon_bytes (1356-1428) vs addr_points footprint pac…` — The addr_points footprint packer inside write_index duplicates pack_polygon_bytes almost exactly: same bbox computation, same 4-tier encoding threshold ladder (65535*1e-6 / 1e-5 / 1e-4 else U32_1CM),… → _Refactor the addr footprint loop to build a verts vector and call pack_polygon_bytes, or extract the shared encoding-selection + header-write into one helper both call. HIGH determinism risk: addr_ve…_
- **MEDIUM** [duplication] `country lookup loop (lines 1129-1140 vs 1159-1170)` — The admin_level==2 country_code lookup from a cell_to_admin entry (mask 0x7FFFFFFF, skip out-of-range, find first level-2 poly with non-zero country_code) is duplicated verbatim for addr_points and f… → _Extract uint16_t country_code_at_cell(const ParsedData&, uint64_t cell_id). Pure read logic, results-sensitive (drives which postcodes get a country and thus pass validation) so keep semantics identi…_
- **MEDIUM** [error-handling] `write_entries (74-76), write_cell_index (176-189), every st…` — No std::ofstream is checked for open failure and no f.write result / f.good() is verified anywhere. On a full disk, bad path, or short write the build silently produces a truncated index and (because… → _Add a small checked-write helper that opens, writes, flushes, and throws on !f or failed write; route all binary emits through it. Pure I/O hardening, no effect on byte output of successful runs, zer…_

_Tests:_ pack_polygon_bytes (and the addr-footprint encoder, ideally after unifying them) (easy); write_entries (the map-based variant) (easy); write_entries_from_sorted vs write_entries equivalence (moderate); simplify_admin_polygon (easy); country_code_at_cell (after extracting the duplicated 1129-1140 / 1159-1170 lookup) (easy); the FNV-1a identity lambda (lines 1267-1272) and packed-id encode/decode (>>56 / &0x00FF...) (easy)

## builder/geocoder_diff.cpp
Command-line tool that computes a custom binary patch between two geocoder index directories (old-dir, new-dir). For each data file it builds a "merge sequence" (MATCH/INSERT/DELETE runs) by walking old (string-remapped, parent-id-remapped, vertex-offset-fixed) and new records in parallel, derives …

**Refactor risk:** HIGH. This file is the determinism-critical producer half of the diff/patch round-trip and almost every line either emits patch bytes (wval/patch.insert) or feeds the matching heuristics that determine those bytes. (1) BYTE-OUTPUT determinism: any change to op/run encoding (MergeSequence add_* 52-67), section ordering (note the explicit ordering constraint at 828-831, and the parent-remap-must-co…

- **HIGH** [structure] `main() lines 708-2637` — main() is ~1930 lines and mixes argument parsing, string-remap construction, stride detection, seven inline thread bodies (t_addr/t_street/t_interp/t_admin/t_poi/t_cells/t_place each 50-130 lines of … → _Extract each thread body into a named free function (e.g. build_addr_merge(...), build_street_merge(...)) taking the shared inputs by reference and returning the FileMergeResult(s). This is behavior-…_
- **HIGH** [duplication] `admin/POI/Place corrections, lines 2199-2322` — Three blocks (admin_entries, poi_entries, place_entries corrections) are ~40 lines each and almost byte-for-byte identical: same parse_ids_v lambda, same parse_<x> lambda, same nm/dm diff loop, same … → _Factor a single helper emit_entry_corrections(fid, derived_cells, derived_entries, new_cells, new_entries) that runs the shared cell-map diff and appends the ENTRY_CORRECTION section. Pass the rebuil…_
- **HIGH** [dead-code] `postcode_id_remap, lines 1142-1149 and 1871` — postcode_id_remap is declared (1149), documented at length (1142-1148) as 'Computed inside t_poi', and emitted as the 'postcode_pairs' block of the POI parent-remap section (1871), but it is NEVER as… → _Confirm with git history / the patcher whether the postcode-pairs block is expected to carry data. If the patcher reads three remap blocks, the empty third block is load-bearing wire structure and mu…_
- **MEDIUM** [dead-code] `streaming_corrections lambda, lines 2051-2091` — next_old (2051-2070), get_derived (2073-2089) and uint64_t d_cid (2091) are defined but never called: the comment at 2093-2096 ('Actually let's simplify') abandons them and re-implements the walk inl… → _Delete next_old, get_derived and d_cid. This is pure dead-code removal with no effect on emitted bytes (they are never invoked). Verify with a compile that nothing references them (they are local lam…_
- **MEDIUM** [duplication] `parse_ids_v lambda, lines 2205-2212, 2247-2254, 2289-2296` — The exact same parse_ids_v lambda (vector<char> overload) is copy-pasted three times in the admin/poi/place correction blocks; parse_ids_raw (2008, MappedFile overload) is a fourth near-copy. Four im… → _Hoist one parse_ids(const char* data, size_t size, uint32_t off) helper near the top and call it from all four sites (MappedFile passes .data/.size; vector passes .data()/.size()). Behavior-preservin…_
- **MEDIUM** [dead-code] `emit_raw_inline lambda, lines 1792-1816` — emit_raw_inline is defined and documented as a 'fallback retained for any file whose byte-block pipeline isn't wired up yet', but grep shows it is never called — emit_raw (2362) is the one actually u… → _Remove emit_raw_inline if truly unused (confirm via compiler -Wunused or grep). Not byte-affecting since it never runs. If kept intentionally as documentation of the wire format, convert the note int…_
- **MEDIUM** [magic-number] `0xFFFFFFFF / 0xFFFFFFFFu NO_DATA sentinel, throughout (e.g.…` — The NO_DATA sentinel 0xFFFFFFFF is re-declared as a local constexpr in at least five scopes (t_addr 1187, t_poi 1599, t_place 1732, emit_sparse_delta NO_DATA_VAL 2469) and also hard-coded inline in r… → _Define a single constexpr uint32_t NO_DATA = 0xFFFFFFFFu at file scope (or pull from patch_format.h if it already has one) and a separate named constant for the end marker. Behavior-preserving (same …_
- **MEDIUM** [error-handling] `system()/zstd invocation lines 2625-2626, 2634-2635` — The final compression shells out via system("zstd -19 -T0 ... 2>/dev/null") and the cleanup via system("rm -rf ..."), with no check of the return code. If zstd is missing or fails, the tool still ret… → _Check system()'s return value and exit non-zero on failure; initialize cst and verify stat() succeeded before printing. Optionally replace the shell-out with libzstd if linked. This does not change t…_

_Tests:_ build_string_remap (191-213) (easy); build_merge_seq_sorted (74-103) (easy); derive_id_remap_from_merge (583-604) (easy); secondary_match_from_merge (530-580) (moderate); build_string_remap + cross-tier classification (tier_of lambda 881-884) (easy); build_child_merge / build_vertex_byte_merge coalescing (1033-1119, 932-1031) (moderate); emit_sparse_delta change detection (remap_kind 0/1/2/3 inner loops, 2506-2548) (moderate); parse_ids_raw / parse_ids_v (2008-2015) (easy)

## builder/geocoder_patch.cpp + patch_format.h
geocoder_patch.cpp applies a binary diff/patch (produced by geocoder-diff) to a "current" index directory to reconstruct a new index, streaming output to keep RSS low for a planet-scale build. It decompresses the zstd patch, rebuilds the tiered string pools and a global string-offset remap, replays…

**Refactor risk:** HIGH. This module is the read/apply half of the determinism + diff/patch lockstep contract, so almost everything here is either determinism-sensitive or parity-sensitive. Risks to BYTE-IDENTICAL output: (1) any change to record/offset emission order or the +1 remap encoding in the entry pipeline (geo at 950-1007, admin 1017-1071, POI 1073-1188, place 1190-1305) and in the shared rebuild_* helpers…

- **HIGH** [dead-code] `patch_format.h:277 (GCPATCH_VERSION) vs geocoder_patch.cpp:…` — GCPATCH_VERSION is defined as 1 in the shared header but is never used; the diff tool hardcodes version 2 (geocoder_diff.cpp:815) and the patch tool hardcodes the check `ver != 2` (line 112). The one… → _Set GCPATCH_VERSION to 2 and have both the diff (write) and patch (check) sites reference the constant instead of a literal. Behavior-preserving: the emitted/checked value stays 2; only the literal i…_
- **HIGH** [magic-number] `geocoder_patch.cpp:128, 247, 302 (string-section markers 0x…` — The string-diff section markers are raw hex literals in the patch tool, while every other marker (CELL_CHANGES_*, ENTRY_CORRECTION_MARKER, etc.) has a named constant in patch_format.h. Worse, 0xFFFFF… → _Add named constants in patch_format.h (e.g. STRINGS_TIERED_MARKER, STRINGS_REMAP_MARKER, STRINGS_LOOP_MARKER) and use them at all three sites. If the tiered-strings marker truly must differ from SECO…_
- **HIGH** [duplication] `geocoder_patch.cpp:1073-1188 (POI rebuild) and 1190-1305 (P…` — The POI and Place entry-rebuild blocks are ~115-line near-verbatim copies of each other (same cell parse, remap, add/remove, corrections-vs-no-corrections write paths), and both duplicate the logic a… → _Collapse the two inline blocks into one shared helper parameterized by file_id/filenames, ideally the existing rebuild_*_from_remap helpers plus a single shared 'apply entry corrections + stream to d…_
- **MEDIUM** [duplication] `patch_format.h:474-649 (rebuild_geo_from_remap vs rebuild_g…` — rebuild_geo_from_remap and rebuild_geo_from_remap_vec are ~85-line near-identical functions differing only in the remap lookup (hash map vs dense vector). The parse_entry lambda, cell parsing, add/re… → _Template the remap step (pass a callable `remap_ids`) and share the rest of the body, or have the map-based overload build a small adapter. Keep the emitted byte order identical; validate against the…_
- **MEDIUM** [structure] `geocoder_patch.cpp:78-1316 (main)` — main() is ~1240 lines doing decompress, header parse, tiered-string rebuild, marker dispatch, merge replay, and the four-part entry pipeline inline. The marker-dispatch chain (lines 302-452) and the … → _Extract cohesive units as free functions taking explicit params: parse_string_section, parse_metadata_markers, replay_merge_section(file_id,...), run_geo_entry_pipeline(...), and one shared rebuild_c…_
- **MEDIUM** [error-handling] `geocoder_patch.cpp:40 (get_rss_mb fscanf) and :92,1311 (sys…` — Multiple unchecked syscalls/library calls: get_rss_mb ignores fscanf's return (the diff tool's copy at geocoder_diff.cpp:38 was hardened to check `!= 2`, so the two have drifted); fstat() returns are… → _Check fstat/ftruncate/mmap/msync returns and fail loudly (the COPY_OLD path at 476-487 already models the right 'surface it loudly' pattern). At minimum mirror the diff tool's fscanf hardening so the…_
- **MEDIUM** [error-handling] `geocoder_patch.cpp:79-83, 84-86, 1311` — argv parsing only validates `argv[3] == "-o"` and argc>=5; out_dir/cur_dir/patch_path are taken positionally with no existence checks. tmpdir is built from getpid() and removed via `system("rm -rf '"… → _Replace the system()-based mkdir/rm with mkdir()/std::filesystem (or nftw-based removal) to avoid shell quoting entirely, and validate cur_dir/patch_path exist up front with clear messages. Behavior-…_
- **MEDIUM** [bug] `geocoder_patch.cpp:457 (`if (file_id >= COUNT) ... break;`)` — On an unknown file_id the loop does `break`, silently stopping all further section processing and then proceeding to the entry pipeline and a `return 0` success. A truncated/garbled patch, or one emi… → _Return a non-zero exit on unknown file_id (or on any `break` out of the section loop that isn't the explicit 0xFFFFFFFF terminator). Pure failure-path change; valid patches still exit via the 0xFFFFF…_
- **MEDIUM** [error-handling] `geocoder_patch.cpp:106-107, 298-299 (ru32/ru64 lambdas, no …` — ru32/ru64 and the many `memcpy(&x, P+pos, n); pos+=n` reads advance `pos` with no check against patch_size, except the top-level `while (pos < patch_size)`. A malformed/truncated section length (e.g.… → _Add a bounds-checked reader (assert pos+n <= patch_size before each read, fail loudly otherwise) and clamp section lengths against remaining bytes. CI patches are trusted, so this is robustness rathe…_
- **MEDIUM** [naming] `geocoder_patch.cpp:621-633 (remap_offs) and 730-784 (inline…` — The per-record field offsets that must / must not be string-remapped are encoded as scattered magic numbers with the rationale only in prose comments (e.g. AddrPoint {8,12} not {8,12,16} at 615-621; … → _Introduce named constants (in patch_format.h next to the Patch* struct definitions, e.g. ADDRPOINT_OFF_HOUSENUMBER_ID/_STREET_ID/_PARENT_WAY/_VERTEX, POI_OFF_NAME/_PARENT_STREET/_PARENT_POSTCODE/_PAR…_

_Tests:_ write_varint / read_varint (patch_format.h:847-864) (easy); FixupDecoder (geocoder_patch.cpp:586-608, currently a local struct) (moderate); RemapRef::operator[] and the +1 encoding (geocoder_patch.cpp:860-869; encode at 791/399, decode at 1025/1082/1199) (easy); str_remap_lookup / poi_remap_lookup (geocoder_patch.cpp:268-273, 288-295) (easy); rebuild_admin_from_remap (patch_format.h:656-717) (moderate); rebuild_poi_from_remap / rebuild_place_from_remap (patch_format.h:725-843) vs the inline POI/Place blocks (geocoder_patch.cpp:1073-1305) (moderate); to_grid (patch_format.h:868-870) (easy)

## builder/pbf_reader.cpp/.h
Parses OSM PBF planet files into in-memory structures for the index builder. Scans the file for blob offsets, reads + zlib-decompresses each blob, and decodes PrimitiveBlocks (string table, dense nodes, ways, relations) into PbfBlock structs or via streaming callbacks. Provides a parallel/threaded …

**Refactor risk:** HIGH for both invariants. This module produces every coordinate, id, ref, and tag that flows into the byte-identical on-disk index, and its tag association feeds Nominatim-parity field selection. DETERMINISM-SENSITIVE: the repeated 1e-9 coordinate arithmetic (385-386, 600-601, 761-762) must remain bit-identical in expression form; entity ordering (push order of nodes/ways/relations and the flat w…

- **HIGH** [duplication] `decode_pbf_blob (300-523), decode_pbf_blob_into (687-845), …` — Four functions re-implement the same PBF traversal: identical string-table parsing (STRINGTABLE/S loop), identical granularity/lat_offset/lon_offset extraction, and largely identical dense-node / way… → _Extract shared helpers: a parse_block_header(reader)->{string_table, granularity, lat_offset, lon_offset}, a decode_dense_nodes(view-set, emit) that takes a per-node sink, a decode_way(reader, emit),…_
- **HIGH** [error-handling] `read_and_decompress_blob_into (178-188) and decompress_blob…` — read_and_decompress_blob_into ignores the return value of inflate() entirely (line 186) and resizes out to strm.total_out regardless of whether inflation reached Z_STREAM_END or errored. A truncated/… → _Capture `int ret = inflate(&strm, Z_FINISH);` and `if (ret != Z_STREAM_END) throw`, matching read_and_decompress_blob and decompress_blob_from_mmap. Also check inflateInit/inflateReset return codes. …_
- **MEDIUM** [dead-code] `decompress_blob_from_mmap (251-296)` — decompress_blob_from_mmap is static and never referenced anywhere in the file (the mmap path was removed per the PbfFile header comment 'mmap removed'). It is a third copy of the decompress logic kep… → _Delete the function. Pure dead-code removal, no behavioral impact on build output or geocoding._
- **MEDIUM** [dead-code] `read_blocks tail (1314-1320)` — After the timing report there is `threads.clear(); return;` followed by an unreachable `for (auto& t : threads) t.join();`. The comment 'Skip the duplicate join below' admits this is leftover. The un… → _Remove the `threads.clear(); return;` and the trailing unreachable for-loop. The real join already happened at line 1296. Pure cleanup; no behavior change._
- **MEDIUM** [duplication] `read_nodes_streaming (956-1019) vs read_ways_streaming (102…` — These two methods are near-identical: same data_indices collection, same BUILD_MAX_IO env parsing, same IoSemaphore setup, same thread loop and timing accumulation. They differ only in the decode fun… → _Factor a private template/helper run_streaming(decode_fn, label) that both call. Also the data_indices(OSMData) collection is duplicated a third time in classify_blobs (1111-1113) and read_pbf_parall…_
- **MEDIUM** [magic-number] `node.lat/lng at 385-386, 600-601, 761-762` — The literal 0.000000001 (nanodegree scale, 1e-9) is hand-written six times across the decode paths. A typo in any one copy (an extra/missing zero) would silently corrupt coordinates for one decode pa… → _Define `static constexpr double NANO_DEG = 1e-9;` (or 0.000000001) once and reuse. Keep the exact same expression form `NANO_DEG * (offset + (int64_t)granularity * coord)` so the floating-point resul…_
- **MEDIUM** [error-handling] `read_pbf_parallel thread lambda (884) and all PbfFile worke…` — Every worker thread does `if (local_fd < 0) return;` and silently abandons its share of blobs. If one thread fails to open the file, a subset of the PBF is silently skipped and the build completes wi… → _Record the failure (atomic flag / captured exception) and rethrow after join(), or at minimum log to std::cerr. Behavior-preserving on the happy path; only changes the failure mode from silent-corrup…_
- **MEDIUM** [bug] `decode_nodes_streaming kv loop (586-598) vs decode_pbf_blob…` — The dense kv (keys_vals) delimiter handling differs subtly between copies. In decode_pbf_blob (and _into) the kv parsing only runs `if (kv_ptr < kv_end)`; nodes with no tags rely on consuming the sin… → _This is the strongest argument for the dedup in the first finding: unify the kv-delimiter walk into one decode_dense_nodes helper so all paths share identical tag-association semantics. Before refact…_

_Tests:_ read_u32_be (87-94) (easy); PbfTags::get (h:22-30) (easy); PbfRelation::member_role (h:65-70) (easy); dense-node coordinate arithmetic (the NANO_DEG * (offset + granularity*delta) expression) (moderate); decode_pbf_blob / decode_nodes_streaming / decode_ways_streaming (differential) (moderate); scan_pbf_blobs (98-147) (moderate); relation member type mapping (502-508 / 833-836) (easy)

## builder/types.h + parsed_data.h
types.h defines the on-disk binary record structs (WayHeader, AddrPoint, PostcodeCentroid, InterpWay, AdminPolygon, PlaceNode, PoiRecord, polygon vertex header layout) shared with the Rust server's index reader, plus enums (AdminPlaceType, VertexEncoding, PlaceType, PoiCategory) and the POI classif…

**Refactor risk:** HIGH. Both files are squarely on the determinism + parity critical path. (1) Determinism / byte-identical index: every struct in types.h is serialized verbatim and read by the Rust server, so any field reorder, type change, padding change, or even reusing an enum numeric value changes on-disk bytes (and breaks the ~243-byte same-PBF patch / 26-26 check). The PoiCategory/AdminPlaceType/VertexEncod…

- **HIGH** [structure] `parsed_data.h:279-410 (partition_strings_into_tiers)` — partition_strings_into_tiers is a single ~130-line function doing five distinct jobs: (1) split the flat pool into (offset,ptr) records, (2) build the consumer tier_mask by scanning every record vect… → _Decompose into named helpers: collect_tier_masks(data, tier_mask), build_tier_buffers(strings, home_tier, ...), remap_record_name_ids(data, remap). Critically, drive both the mark and remap passes fr…_
- **MEDIUM** [structure] `types.h:14-22 (AddrPoint), 8-12 (WayHeader), 26-32 (Postcod…` — AdminPolygon (109) and PoiRecord (921) have static_assert size guards, but the other on-disk structs that the Rust server reads byte-for-byte (WayHeader, AddrPoint, PostcodeCentroid, InterpWay, Place… → _Add static_assert(sizeof(T) == N, ...) for every struct that is serialized to a .bin and read by the server (WayHeader, AddrPoint, PostcodeCentroid, InterpWay, PlaceNode), mirroring the AdminPolygon/…_
- **MEDIUM** [magic-number] `types.h:122-126 vs 222,897,906,914; parsed_data.h:258,296,3…` — NO_DATA is defined as 0xFFFFFFFFu (types.h:126) but many sites both define field defaults and test sentinels with the raw literal 0xFFFFFFFF/0xFFFFFFFFu instead of the named constant (PlaceNode.paren… → _Use the NO_DATA constant for all default initializers and drop the redundant second comparison (the '|| off == 0xFFFFFFFFu' clauses are dead/duplicate). Behavior-identical; reduces drift risk if the …_
- **MEDIUM** [structure] `types.h:355-472 (poi_get_default_tier), 475-524 (poi_get_pr…` — Six large hand-maintained switch tables key off the same ~200-value PoiCategory enum, each with its own grouping comments and 'default:' fallbacks. They overlap heavily (e.g. the containment list of … → _Consolidate the per-category metadata into a single source of truth: one static constexpr table indexed by PoiCategory (struct { tier; proximity_m; base_importance; ref_dist; max_dist; const char* la…_

_Tests:_ partition_strings_into_tiers (parsed_data.h:279) (moderate); ParsedData::get_string (parsed_data.h:257) (easy); deduplicate (parsed_data.h:414) (easy); poi_get_default_tier / poi_category_label / poi_get_proximity_meters / category_base_importance / category_reference_distance / category_max_distance (types.h) (moderate); ensure_dir (parsed_data.h:26) (easy)

## geocoder/builder pure helpers (ring_assembly, s2_helpers, interpolation, continent_filter, continent_boundaries, cache, geometry.h, postcode_validation.h, string_pool.h, id_allocator.h, relation_collector.h)
These files form the builder's geometry/identity helper layer. geometry.h holds pure computational primitives (coord hashing, shoelace area, segment-intersection, self-intersection, Douglas-Peucker simplification, postcode/highway validation, ICU-approximate string normalisation, house-number parsi…

**Refactor risk:** HIGH for the geometry/identity core; MEDIUM-LOW for the leaf validators. Concretely: DETERMINISM (invariant #1) — these touch on-disk byte layout / id assignment and must be treated as golden: - ring_assembly.cpp (ring vertex ordering, the rotate-to-min-vertex normalisation in add_admin_polygon at s2_helpers.cpp:298-300, append order in greedy/backtrack passes) feeds simplify -> admin_vertices.bi…

- **HIGH** [structure] `continent_filter.cpp:filter_by_bbox_masked (35-529)` — Single ~500-line function doing ID collection, six parallel record remaps (ways/addrs/interps/admin/pois/places), parent-chain projection, postcode projection, cell-map remap, string-pool compaction,… → _Extract a templated remap_records<RecordT> helper parameterised by an optional point-in-polygon predicate and an optional vertex-carry functor, and a remap_parent_array helper for the parallel-array …_
- **MEDIUM** [duplication] `continent_filter.cpp:51-95 and 379-415` — filter_sorted_masked and remap_sorted_masked share the exact same thread-chunking/boundary-finding logic (the cell-id-aligned `bounds` computation at 60-67 and 386-392 is byte-for-byte identical), di… → _Extract a `partition_sorted_by_cell(sorted, nthreads) -> vector<size_t> bounds` helper and call it from both. Pure index arithmetic, no determinism impact (it only controls thread partition boundarie…_
- **MEDIUM** [dead-code] `geometry.h:239-264` — kDefaultEpsilon[] (239-252) appears unused: admin_epsilon_meters (261) reads kAdminEpsilonMeters[] instead, and the two tables hold the same values. kDefaultEpsilon is a likely-stale duplicate. → _Confirm with grep that kDefaultEpsilon has no readers, then delete it. If it is the documented 'defaults' source for kAdminEpsilonMeters, replace kAdminEpsilonMeters's literal initialiser with a copy…_
- **MEDIUM** [bug] `geometry.h:359-363 normalise_for_matching` — For a 3/4-byte UTF-8 lead byte it advances p by skip without verifying continuation bytes exist; a truncated/invalid multibyte sequence at end-of-string can advance p past the NUL terminator and read… → _Bound the skip to not pass the terminator (stop at first NUL among the continuation bytes). Results-sensitive (street-name matching → Nominatim parity), so verify normalised output is unchanged for a…_
- **MEDIUM** [structure] `ring_assembly.cpp:assemble_outer_rings (6-203)` — ~200-line function with three distinct phases (split ways at shared nodes → build adjacency → greedy stitch → backtracking retry) inline, plus a nested BacktrackState struct. The greedy extension loo… → _Extract `append_subway(ring, coords, reversed)` for the forward/reversed-skip-first-vertex append used in 3 places (137-138, 184-185), and split the function into split_into_subways(), build_adjacenc…_
- **MEDIUM** [duplication] `s2_helpers.cpp:cover_polygon (76-151)` — The 'expand a coarse cell to all kAdminCellLevel descendants via range_min..range_max, else clamp to parent' block is written twice — once to build interior_set (105-117) and once to build result (11… → _Extract `for_each_admin_cell(cellUnion, fn)` that yields each kAdminCellLevel cell id and call it twice; replace the manual dedup with sort + a small merge helper. Determinism-sensitive (admin cell c…_
- **~~MEDIUM [bug]~~ → STALE / NOT A BUG (verified)** `cache.cpp:7-16 vs s2_helpers / continent_filter` — SectionType enum (7-11) lists 13 sections and CACHE_SECTION_COUNT=13, but serialize_cache (98-109) only writes 12 sections… → _Either count sections written dynamically or add a static_assert…_
  - ✅ **CORRECTION:** current `serialize_cache` (cache.cpp:97-109) writes all 13 sections, matching `CACHE_SECTION_COUNT=13`. No off-by-one. Only a cosmetic NOTE remains (optionally bind the constant to the write count with a static_assert).
- **MEDIUM** [structure] `continent_boundaries.cpp:39-52 + continent_filter.cpp:13-22` — The continent list exists in two parallel hand-ordered tables (kContinents[] bbox in continent_filter.cpp and defs[] name-mapping in continent_boundaries.cpp) with a comment 'Order must match kContin… → _Define a single source-of-truth array of {name, embedded_name, bbox} and derive both views from it, or add a static_assert / runtime check that the two name lists match element-for-element at startup._

_Tests:_ geometry.h:is_valid_postcode (easy); postcode_validation.h:validate_postcode_for_country (easy); geometry.h:parse_house_number (easy); geometry.h:normalise_for_matching and tokens_overlap (easy); geometry.h:is_included_highway / is_included_highway_full (easy); geometry.h:polygon_area and ring_has_self_intersection / ring_has_duplicate_coords (easy); geometry.h:dp_simplify / simplify_polygon / simplify_polygon_epsilon (moderate); geometry.h:coord_key + segments_intersect (easy); id_allocator.h:IdAllocator (load_previous/allocate/finalize/take_slots) (moderate); string_pool.h:StringPool::intern (easy); continent_boundaries.h:point_in_polygon / point_in_polygon_nc (easy); geometry.h:meters_to_degrees + admin_epsilon_meters (easy); cache.cpp:serialize_vector/deserialize_vector and serialize_cell_map/deserialize_cell_map (moderate)

## builder aux tools (geocoder_canonicalize.cpp + simplify_compare.cpp)
geocoder_canonicalize.cpp is an offline CLI that rewrites a built geocoder index into a deterministic "canonical" form: it alphabetically sorts the string pool, content-sorts each data file (addr_points, street/interp ways+nodes, admin polygons+vertices), remaps string offsets and record IDs, and r…

**Refactor risk:** MEDIUM-to-HIGH, and the two files differ sharply. geocoder_canonicalize.cpp is directly determinism-sensitive: it rewrites the exact on-disk byte layout (record ordering, string-pool offsets, id remaps, entry/cell framing). Its whole purpose is to make same-PBF builds compare byte-identically, so any change to sort comparators, stride/offset guessing (lines 353/417-418/484-488), the offset-0 map(…

- **HIGH** [bug] `geocoder_canonicalize.cpp:58-61 (CanonicalStringPool::map)` — map() returns 0 both when an offset is found-mapped-to-0 (the alphabetically-first string lands at offset 0) and when the old offset is NOT in the remap table. There is no way for callers to distingu… → _Reserve offset 0 for an empty sentinel (or store remap presence separately) and have map() assert/log on miss. Behavior-preserving option: keep returning a defined value but emit a one-line warning t…_
- **HIGH** [magic-number] `geocoder_canonicalize.cpp:353,417-418,484-488,370,436,504` — Record strides are guessed at runtime from file-size divisibility (way_stride 12-or-9, interp stride 20/18, admin stride 24/20/19) and field offsets are then branched on the guessed stride (name_off … → _Derive the authoritative struct stride/field offsets from patch_format.h (the read_ways/read_interps/read_admin_polygons helpers already know the true layout) rather than re-deriving from file size. …_
- **HIGH** [bug] `simplify_compare.cpp:178,187,269,283 (poly_verts[i][0].lat)` — meters_to_deg uses poly_verts[i][0].lat, indexing vertex 0 unconditionally. A polygon with zero vertices (vertex_count==0) reads out of bounds; simplify_max/simplify_eps also index pts[pts.size()-1] … → _Guard for empty polygons before dereferencing vertex 0 / pts.size()-1 (skip or treat as already-simplified). Tool-only, but a single empty admin record crashes the run._
- **MEDIUM** [duplication] `geocoder_canonicalize.cpp:151-191 vs 193-249, 207-226 vs 16…` — write_entries_and_cells and write_geo_cells_and_entries contain near-identical entry-serialization logic (count u16 + ids u32 array, offset bookkeeping, 0xFFFFFFFF no-data). The geo variant duplicate… → _Extract a single serialize_entry(buf, ids)->offset helper and a write_cells(path, sorted_cells, offset_lookup, stride) helper; have both writers call them. Pure byte-for-byte equivalent extraction, l…_
- **MEDIUM** [dead-code] `geocoder_canonicalize.cpp:155,533-534 (cell_entry_size / tr…` — write_entries_and_cells takes a cell_entry_size parameter (documented '20 for geo, 12 for admin') but never uses it inside the function body; the caller passes 12. Dead parameter that misleads reader… → _Remove the unused cell_entry_size parameter and its argument at the call site._
- **MEDIUM** [magic-number] `geocoder_canonicalize.cpp:82-90 (parse_entry), 101,122 (n_g…` — Entry framing (offset 0xFFFFFFFF sentinel, 2-byte count, 4-byte ids) and cell record sizes (20 for geo, 12 for admin) are bare literals scattered across parse_entry, extract_cell_assignments, and bot… → _Name constants: GEO_CELL_RECORD_BYTES=20, ADMIN_CELL_RECORD_BYTES=12, ENTRY_COUNT_BYTES=2, ENTRY_ID_BYTES=4, NO_DATA_OFFSET=0xFFFFFFFFu. Reuse in parse and write paths._
- **MEDIUM** [error-handling] `geocoder_canonicalize.cpp:267,275,280,322-323 etc. (all rea…` — No file existed/size-sanity checks. read_file presumably returns empty on missing file; n_geo=size/20, n_admin=size/12 etc. silently truncate on a partial file, and a missing input file would produce… → _Validate each input file exists and its size is an exact multiple of the expected record stride; abort with a clear message otherwise. Read-only check, does not change output bytes for valid inputs._
- **MEDIUM** [duplication] `simplify_compare.cpp:12-22` — AdminPolygon is redeclared locally with a hardcoded layout and static_assert(sizeof==24), duplicating PatchAdminPolygon in patch_format.h. If the canonical struct layout changes, this tool silently r… → _Include patch_format.h and reuse its admin-polygon reader/layout, or at minimum add a comment that this struct must track patch_format.h::PatchAdminPolygon. Given it is a throwaway analysis tool the …_
- **MEDIUM** [error-handling] `simplify_compare.cpp:109-112 (load lambda)` — The load lambda never checks that the ifstream opened or that read() succeeded; f.tellg() on a failed open returns -1 which becomes a huge size_t in buf.resize(), causing a bad_alloc/throw with no di… → _Check f.is_open() and tellg()>=0, print a clear error and exit(1) on failure; clamp/validate vertex ranges before assign._

_Tests:_ CanonicalStringPool::build / map (geocoder_canonicalize.cpp:31-61) (easy); parse_entry (geocoder_canonicalize.cpp:82-90) (easy); remap_cells (geocoder_canonicalize.cpp:136-148) (easy); to_grid (patch_format.h:868) (easy); simplify_max / simplify_eps / dp_dist (simplify_compare.cpp:26-82) (easy); meters_to_deg / admin_eps_meters (simplify_compare.cpp:84-96) (easy)

## server/lib.rs (query engine, 4289 lines)
Loads the per-continent binary index via mmap (FileBytes abstraction over Owned/Mmap/JsChunked), exposing zero-copy typed reads. Answers reverse-geocode queries: Index::query() orchestrates geo lookup (streets/addr/interp), POI primary selection, admin boundary resolution (PIP + parent-chain), plac…

**Refactor risk:** HIGH for the query path, LOW for the leaf helpers. This module does NOT write the index (no build-output-byte determinism risk here — that invariant lives in the C++ builder). The dominant risk is NOMINATIM RESULTS PARITY: Index::query, find_admin, find_admin_from_chain, find_places, find_nearest_poi_with_parent, resolve_postcode, and the geometry/field-mapping helpers all directly determine the …

- **HIGH** [structure] `Index::query (lines 3070-3570)` — query() is a ~500-line monolith mixing at least six distinct phases: geo/admin/place lookups, POI primary candidate gating (effective_poi_dist, tier1 boost, containment demotion), primary-feature win… → _Extract behavior-preserving private helpers that return owned values: select_primary_feature(addr,interp,street,poi_primary,max_dist) -> (road, house_number, addr_won, poi_won); compute_effective_poi…_
- **HIGH** [duplication] `Index::find_admin (1721-2123) and Index::find_admin_from_ch…` — The two functions duplicate ~150 lines nearly verbatim: identical RankedPoly/RankedPoly2 + FieldCandidate/FC structs, the identical field_index match arm, the identical 3-iteration rank-adjustment ca… → _Lift the shared pieces into free functions/types at module scope: a single RankedPoly struct, fn field_index(&str)->Option<usize>, fn apply_rank_cascade(&mut [RankedPoly], &[place_area]), fn assign_f…_
- **MEDIUM** [duplication] `query_geo closures (1327-1363) and find_addr_on_way closure…` — lookup_geo_chunked (20-byte-stride geo-cell binary search) and read_entries (u16 count + u32 ids blob reader) are copy-pasted identically into both query_geo and find_addr_on_way as local closures. T… → _Promote a single Index::lookup_geo_cell_fb(&FileBytes, u64) -> GeoCellOffsets and reuse for_each_entry_fb (or a read_entries_fb returning Vec<u32>) in both query_geo and find_addr_on_way. Pure byte-d…_
- **MEDIUM** [duplication] `Index::find_admin best_by_field assignment (2095-2120); fin…` — The 18-line 'result.state = best_by_field[1]...; result.city_block = best_by_field[17]...' block is written out twice verbatim, including the identical country_code [hi,lo] byte split. Any new field … → _Extract fn admin_result_from_fields(fields: [Option<FieldCandidate>;19]) -> AdminResult once and call from both. The numeric slot<->field coupling is fragile; consider a single source-of-truth array …_
- **MEDIUM** [naming] `Index::query_geo signature & return (1295-1297, 1486)` — The return tuple is (addr, interp, street) but the type annotation orders the elements (addr:AddrPoint-tuple, street-named-but-actually-interp &str tuple, interp-named-but-actually-street WayHeader t… → _Return a small named struct GeoQueryResult { addr: Option<(f64,AddrPoint,u32)>, interp: Option<(f64,&str,u32)>, street: Option<(f64,WayHeader,u32)> } so field access is self-documenting and mis-bindi…_
- **MEDIUM** [magic-number] `Magic constants INTERIOR_FLAG/ID_MASK and ad-hoc radius squ…` — const INTERIOR_FLAG: u32 = 0x80000000 and ID_MASK = 0x7FFFFFFF are redeclared locally in at least 6 functions (find_admin 1729, debug_admin 1664, find_current_boundary 2135, find_municipality_boundar… → _Define module-level pub const ADMIN_INTERIOR_FLAG / ADMIN_ID_MASK and pub const EARTH_RADIUS_M: f64 = 6_371_000.0, plus a helper fn meters_to_rad_sq(m: f64) -> f64. Replace inline copies. These are e…_
- **MEDIUM** [error-handling] `FileBytes typed reads: decode_polygon_verts (199-223), quer…` — Many unsafe from_raw_parts/read_unaligned casts assume the file length is an exact multiple of sizeof(T) and that offsets are in-bounds; record_count (167) truncates with integer division and several… → _Add a single checked helper (e.g. read_records_checked returning Option) used on the hot paths, or validate file_len % size_of == 0 at load time and bounds-check node_offset*stride+count against the …_
- **MEDIUM** [readability] `Index::find_admin (1751-1789) interior-flag verification br…` — The nested best_by_level update logic (1751-1787) has four interleaved cases (larger+verified, larger+unverified-needs-recheck, smaller+pip, no-existing) with `best_by_level[level].unwrap()` re-reads… → _Extract a pure fn consider_admin_candidate(state: &mut Option<(f32,AdminPolygon,bool)>, cand: AdminPolygon, is_interior: bool, pip: impl Fn(&AdminPolygon)->bool). Keep the exact branch semantics; uni…_

_Tests:_ format_address (4184) + format_rules (4133) (easy); format_postcode (3875), postcode_looks_valid (3837), country_has_no_postcode (3818), centroid_postcode_ok (3854) (easy); place_type_to_field / rank_to_field / place_type_to_rank / poi_category_to_osm_class / is_area_label_category (easy); address_score (3792) (easy); point_in_polygon (3982), polygon_distance_sq (3964), point_to_segment_with_t (3929), dist_sq (3925) (easy); ContinentBBox::contains (886) + CONTINENTS dispatch (easy); Index::lookup_geo_cell / lookup_admin_cell / for_each_entry (1230-1291) and *_fb variants (moderate); decode_polygon_verts (177) + vertex_stride/vertex_scale (moderate); extracted select_primary_feature / compute_effective_poi_dist (proposed extraction from query) (hard)

## server infra (main/auth/downloader/region_state/wasm)
HTTP server wiring (main.rs) exposing /reverse, /polygons, /test, /admin/configuration plus an embedded auth dashboard (auth.rs). Auth.rs is a self-contained user/token/session store with bcrypt password hashing, an atomic-counter rate limiter, and HTML dashboard rendering. downloader.rs resolves a…

**Refactor risk:** Low overall for this module with respect to BOTH hard invariants. This is server runtime/infra code: it does not produce the on-disk index and does not participate in the C++ builder or diff toolchain, so refactoring here cannot change build OUTPUT BYTES — the byte-identical determinism guarantee is not at risk from any change in main.rs/auth.rs/downloader.rs/region_state.rs/wasm_chunked.rs. NOMI…

- **~~HIGH [bug]~~ → NOT REPRODUCIBLE (verified)** `wasm_chunked.rs:54-76 ensure_page` — LRU bookkeeping is inconsistent with the eviction logic and can desync from the cache… → _Replace the hand-rolled Vec+HashMap LRU…_
  - ✅ **CORRECTION:** re-traced current `ensure_page` — lru and cache stay 1:1 (hit path dedups before push; evict path removes one of each). Could not reproduce the described desync; downgrade to not-a-bug (optionally add a debug assert). NOTE: a *separate* real out-of-bounds underflow at wasm_chunked.rs:43 (`file_len - page_off`) and :82 (`off + len`) remains open (MEDIUM) — clamp to file_len.
- **MEDIUM** [error-handling] `wasm_chunked.rs:82 read / fetch_page:43` — `let end = off + len as u64;` and `self.file_len - page_off` can overflow/underflow. If a caller requests `off + len` past EOF, `fetch_page` computes `self.file_len - page_off` which underflows (pani… → _Clamp `end` to `self.file_len` and early-return empty if `off >= file_len`. Behavior-preserving for in-bounds callers; only changes the (currently UB) out-of-bounds path. No byte-layout or geocoding-…_
- **MEDIUM** [structure] `main.rs:32-114 reverse_geocode` — reverse_geocode is ~80 lines mixing five concerns: key extraction, token validation, rate-limit key derivation, on-demand region download, and a large inline debug-dispatch block (lines 75-110). The … → _Extract the debug dispatch (lines 75-110) into `fn debug_response(idx, lat, lon, mode) -> Response` and the auth+rate-limit preamble into a helper returning Result<rate_key, Response>. Pure restructu…_
- **MEDIUM** [duplication] `main.rs:40-58 and polygons_geojson:167-173` — The 'extract key -> 401 if missing -> validate_token -> 401 if invalid' preamble is duplicated between reverse_geocode and polygons_geojson (with a subtle difference: polygons only checks is_none()). → _Extract a `fn require_valid_key(state, params.key) -> Result<TokenInfo, Response>` helper. Behavior-preserving; not results-sensitive._
- **MEDIUM** [readability] `main.rs:90-99 debug 'all' branch` — Round-trips the address through serde twice: `serde_json::from_str(&serde_json::to_string(&address))` to embed it as a Value, with `.unwrap_or_default()` swallowing both errors. This is opaque and si… → _Use `serde_json::to_value(&address).unwrap_or_default()` directly instead of string round-trip. Behavior-preserving (this is a debug-only endpoint, not the parity-tested /reverse JSON which uses quer…_
- **MEDIUM** [structure] `main.rs:253-261, 320-325, 355` — Argument parsing is ad-hoc and scattered: positional args[1]/args[2] mixed with manual flag scanning via `arg_value` closure and separate `args.iter().position` calls for --domain/--cache. Positional… → _Consolidate CLI parsing into one struct built up front (or adopt clap). Behavior-preserving if the same precedence is kept; not determinism-sensitive (CLI only affects runtime params, not build outpu…_
- **MEDIUM** [error-handling] `auth.rs:189-190 login_submit` — First-login-ever auto-creates an admin from whatever credentials are POSTed (`if db.users.is_empty()`), then falls through to verify. This TOCTOU-free but unauthenticated bootstrap is a real security… → _Add an explicit comment is already partially present; consider gating behind an env flag or logging the bootstrap event. Note: changing this is behavior-affecting (not byte/parity, but auth semantics…_
- **MEDIUM** [error-handling] `auth.rs:67-69 Db::save` — save() does `serde_json::to_string_pretty(self).unwrap()` and `fs::write(...).expect(...)` — a serialization or disk error panics the handler thread (and on create_user/create_token paths can take do… → _Return Result from save() and propagate, and use the same tmp-file+rename atomic-write pattern already in downloader.rs:171-177. Behavior-preserving for the success path; no determinism/parity impact._

_Tests:_ downloader::resolve_files (easy); wasm_chunked::JsChunked (read/ensure_page LRU) (moderate); auth::check_rate (moderate); auth::html_escape (easy); auth::get_session_cookie (easy); downloader::DownloadError Display (easy)

## Suspected pre-existing bugs (surfaced while writing the test suite)

All but the first were left UNFIXED because they are behaviour/parity-affecting: changing them alters which postcodes/categories the query path accepts, which must be confirmed against Nominatim's source and re-validated with the 500-city parity test before it can land. The tests assert current behaviour. The first (an alignment UB on a dead code path) was safe to fix and has been.

- **HIGH (server/lib.rs `decode_polygon_verts` encoding 4 / F32) — FIXED:** the zero-copy path did `slice::from_raw_parts(bytes.as_ptr().add(POLY_HEADER_BYTES) as *const NodeCoord, …)`. `POLY_HEADER_BYTES` is 10 (2-aligned), but `NodeCoord` needs 4-byte alignment → undefined behaviour; only "worked" when the mmap base happened to land it 4-aligned. Fixed by decoding each vertex with per-field unaligned `f32::from_le_bytes` reads (bit-identical f32 values on little-endian, no alignment hazard) and added test `decode_polygon_verts_f32_encoding4`. Risk-free: the builder only ever emits encodings 0/1/2/3 (`build_index.cpp:5683-5691`), so encoding 4 is a legacy/compat reader branch that never appears in any index the current pipeline produces — the change cannot alter any current query result.
- **LOW (server/lib.rs `poi_category_to_osm_class`):** value 74 returns `None` (gap between the `73` and `75` arms).
- **LOW (server/lib.rs):** `poi_category_to_osm_class(150)` (POWER_PLANT) returns `Some("landuse")` even though the doc comment says it's "Excluded" — the `142..=152` arm catches it. Comment/code mismatch.
- **LOW (server/lib.rs `postcode_looks_valid`):** the all-same-char placeholder rule rejects a legitimate single-char postcode like `"5"`.
- **LOW (server/lib.rs `centroid_postcode_ok`):** the "PO Box" filter is a case-sensitive substring check, so `"po 1"` slips through.
- **NOTE (builder/postcode_validation.h):** US ZIP+4 like `"12345-6789"` is REJECTED — hyphens are stripped (not treated as a separator), yielding 9 digits which fails the exact-5 rule. Same shape for other exact-length numeric countries.
